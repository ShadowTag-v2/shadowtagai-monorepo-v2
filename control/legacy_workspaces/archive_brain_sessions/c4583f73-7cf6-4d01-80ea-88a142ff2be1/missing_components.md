# Missing Component detector

**Agent:** 3 — Missing Component Detector
**Target:** `shadowtag-omega-v4`

## Implied But Unimplemented Modules

The following subsystems were theoretically invoked or expected by the architecture and thread context, but contain no underlying implementation code yet within the repository matrix:

### 1. RAG Knowledge Store (`rag_engine/`)

- **Missing:** Vector DB bindings (e.g., Pinecone/Weaviate integration).
- **Implied:** The `sequential-thinking` module needs a `memory` endpoint to persist graph traversal history over long timelines, requiring vectorized search rather than simple Key/Value retrieval for deep source correlation.

### 2. Policy Objection Engine (`policy_engine/objection/`)

- **Missing:** The actual programmatic API that evaluates a PR diff and throws a rejection error based on non-regex constraints (like intent alignment or legal phrasing).
- **Implied:** We have the structural "Gate 0" regex linter, but we lack the semantic "Gate 1" Judge #6 verification API script.

### 3. Jurisdiction Rules Engine (`core/jurisdiction/`)

- **Missing:** Logic that restricts actions based on the geographical deployment zone (e.g., stopping data export to non-GDPR zones).
- **Implied:** Mentioned heavily in COR.30 constraints regarding user access and data sovereignty, but completely absent in codebase.

### 4. Evaluation Harness (`agent_engine/validators/eval/`)

- **Missing:** The actual LLM-as-a-Judge test suites that mathematically grade the Swarm's output against the 93% success rate target.
- **Implied:** Referenced heavily in the Semi-Formal Reasoning model requirements.

### 5. Telemetry & Prompt Registry (`observability/` & `prompt_registry/`)

- **Missing:** Winston/Sentry JSON log adapters and the version-controlled registry DB tracking which agent prompt produced which output.
- **Implied:** COR.30 requires structured logs with PII stripped, but no stripping middleware pipeline has been initialized.

### 6. Artifact Verification System (`agent_engine/verification/`)

- **Missing:** The pipeline that cryptographically signs the generated Markdown artifacts (like this one) to prove they were generated by a specific agent within a secure enclave.

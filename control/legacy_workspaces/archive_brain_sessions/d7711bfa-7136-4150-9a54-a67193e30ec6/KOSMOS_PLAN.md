# KOSMOS INTEGRATION PLAN (GOD MODE)

## 1. The "Real Moat" Implementation
We are deploying the **Persistent Agent Evolution System** (Whiteboard + Bar Exam).
This turns the repo into a living brain that upgrades itself with every task.

### Target Architecture
*   **Repo:** `ShadowTag-v2`
*   **Service:** `apps/n-autoresearch/Kosmos/BioAgentss-server` (The Swarm) OR `apps/judge-sentinel` (The Brain).
*   **Path:** `src/agents/`

## 2. Core Files to Ship
1.  **`legal_whiteboard.py`**: A GitHub-backed JSON state store.
2.  **`bar_exam_protocol.py`**: The logic that promotes the agent from Level 0 -> 5.
3.  **`model_router.py`**: The FLASH (90%) / PRO (10%) routing logic.

## 3. Integration Strategy
*   We will place these in `apps/judge-sentinel` as it is the "Sovereign" core.
*   We will ensure the `whiteboard` directory is git-tracked (so state persists).

## 4. Doctrine Update
*   Ingest "ShadowTagAI Master Handoff" into `GEMINI.md` as the supreme law.

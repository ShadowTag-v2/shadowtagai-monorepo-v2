# Workflow: ShadowTag Omega V7 (Singularity Loop)

**Trigger:** `/omega-loop`

**Directives:**

1.  **Recall (The Link):**
    - Run `python3 tools/beads_core.py` (BeadsEngine).
    - _Context:_ "You are resuming from this state."

2.  **Think (The Cortex):**
    - Load `MirasCore` (Huber Loss Logic).
    - Optimize decision path.

3.  **Execute (The Hands):**
    - **Research:** `curl localhost:3000/tool/browse` (Jetski).
    - **Code:** `JudgeSix.vet(code)`.
    - **Commit:** `BeadsEngine.log("Action Completed")`.

4.  **Loop:**
    - Recursion.

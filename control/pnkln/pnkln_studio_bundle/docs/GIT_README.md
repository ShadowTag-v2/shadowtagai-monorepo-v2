# PNKLN Git Tree

This directory structure mirrors a repo-ready layout for the Vertex AI deployment.

## Branching / Sync Notes
- `main` branch → canonical release
- `vertex/` tags for production-ready bundles
- Run `git add . && git commit -m "update"` to snapshot

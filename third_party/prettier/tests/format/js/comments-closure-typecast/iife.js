const helpers1 = /** @type {Helpers} */ (((helpers = {}) => helpers)());

const helpers2 = /** @type {Helpers} */ ((() => something)());

const helpers = /** @type {Helpers} */ (
  /** @param {Partial<Helpers>} helpers */
  ((helpers = {}) => helpers)()
);

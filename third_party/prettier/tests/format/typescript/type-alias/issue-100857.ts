type FieldLayoutWith<T extends string, S = { width: string }> = {
  type: T;
  code: string;
  size: S;
};

type FieldLayoutWith<T extends string, S> = {
  type: T;
  code: string;
  size: S;
};

type FieldLayoutWith<S = { width: string }> = {
  type: T;
  code: string;
  size: S;
};

type FieldLayoutWith<T extends stringggggggggggg, T extends stringggggggggggg> = {
  type: T;
  code: string;
  size: S;
};

type FieldLayoutWith<T extends stringggggggggggg, S = stringggggggggggggggggg> = {
  type: T;
  code: string;
  size: S;
};

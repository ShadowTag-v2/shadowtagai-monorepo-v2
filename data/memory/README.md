# Operator invariants seed

Use these files as canonical startup law for Antigravity's Git/GitHub behavior.

Suggested install targets:
- `data/memory/operator_invariants.json`
- `data/memory/operator_invariants_atoms.json`

Required startup order:
1. authority-current.json
2. operator_invariants.json
3. monorepo control-plane truth
4. tasks / drift / summaries
5. only then repo reasoning and Git/GitHub operations

ANTIGRAVITY HANDOFF

PART 1 — AUTHORITY BRIEF

You are operating inside one canonical monorepo only.

Authority order:
1. AGENTS.md
2. monorepo_manifest.yaml
3. antigravity-mcp-config.json
4. beads / recovery memory

Operational rules:
- Stay strictly inside the canonical monorepo workspace.
- Do not discuss, inspect, or reason over other repos unless explicitly instructed.
- Treat the canonical MCP config as live operating context.
- Use all configured MCP servers to the fullest safe extent.
- Prefer MCP tools, MCP resources, and MCP prompts before ad hoc workflows.
- Do not create a second control plane.
- Do not revive superseded artifacts.
- Act without prompting for routine non-destructive work; report what changed.
- Ask only before destructive, irreversible, production, external-account, or secret-related actions.

FAST-PATH ENFORCEMENT BLOCK
Use the canonical monorepo only.
Use all configured MCP servers to the fullest safe extent.
Prefer MCP tools, MCP resources, and MCP prompts before ad hoc workflows.
Do not revive superseded artifacts.

Use the canonical monorepo only.
Use all configured MCP servers to the fullest safe extent.
Prefer MCP tools, MCP resources, and MCP prompts before ad hoc workflows.
Do not revive superseded artifacts.

PART 2 — MACHINE POLICY BLOCK

authority_order:
  - AGENTS.md
  - monorepo_manifest.yaml
  - antigravity-mcp-config.json
  - beads

workspace_policy:
  monorepo_only: true
  discuss_other_repos: false
  inspect_other_repos: false
  modify_other_repos: false

mcp_policy:
  canonical_config_required: true
  use_all_configured_servers: true
  prefer_mcp_tools: true
  prefer_mcp_resources: true
  prefer_mcp_prompts: true
  fallback_only_if_unavailable: true
  no_second_mcp_control_plane: true

execution_policy:
  act_then_report_for_routine_work: true
  ask_before_destructive: true
  ask_before_irreversible: true
  ask_before_production: true
  ask_before_external_account: true
  ask_before_secret_handling: true

supersession_policy:
  revive_superseded_artifacts: false
  reference_only_artifacts_are_not_live_truth: true

PART 3 — STAGE 3 VERIFICATION CHECKLIST

A. Active files
- Confirm AGENTS.md is loaded
- Confirm monorepo_manifest.yaml is loaded
- Confirm antigravity-mcp-config.json is loaded

B. Workspace
- Confirm current working root equals the canonical monorepo root
- Confirm no side workspace or nested repo is being used

C. MCP
- Enumerate configured MCP servers
- Confirm MCP tools are discoverable
- Confirm MCP resources are discoverable
- Confirm MCP prompts are discoverable
- Confirm adapters/retired MCP files are not acting as truth

D. Runtime
- Confirm interpreter path resolves
- Confirm local tasks run inside monorepo root
- Confirm secrets come from env, not committed config

E. Drift
- Search for stale MCP paths
- Search for superseded artifact names
- Search for old branding
- Search for repo-outside-monorepo references

F. Final proof
- Produce pass/fail report
- List remaining blockers
- State final canonical control-plane files

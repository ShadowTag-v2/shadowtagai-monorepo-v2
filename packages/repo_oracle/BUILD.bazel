# BUILD.bazel — packages/repo_oracle
# Phase 2: Canonical Bazel package
# Owner: platform

load("@rules_python//python:defs.bzl", "py_library")

package(default_visibility = ["//visibility:private"])

py_library(
    name = "repo_oracle",
    srcs = [
        "__init__.py",
        "oracle.py",
    ],
    visibility = [
        "//packages:__subpackages__",
        "//tools:__subpackages__",
    ],
)

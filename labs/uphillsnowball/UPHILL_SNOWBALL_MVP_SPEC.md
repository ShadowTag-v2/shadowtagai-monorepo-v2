# UPHILL SNOWBALL: MVP ARCHITECTURAL SPECIFICATION

> *"Elegance is achieved when there is nothing left to remove." — Steve Jobs*

**CLASSIFICATION:** TIER 30 // SOVEREIGN EYES ONLY
**PROJECT / ROOT:** `shadowtag-omega-v4` / `labs/uphillsnowball`
**RUNTIME ENVIRONMENT:** Local Apple Silicon (MacBook / ANE) + ShadowTag Cloud
**MODEL:** `gemini-3.1-flash-lite-preview`
**STATUS:** GOD MODE ACTIVE (IQ 160 LOCK)

## ⏺ ///▙▖▙▖▞ OVERVIEW & BUSINESS ROLE
While `apps/counselconduit` serves as the commercial wedge, **Uphill Snowball** (`labs/uphillsnowball`) is the Sovereign Internal R&D Lab.

It is the absolute vanguard of the Omega Protocol. Uphill Snowball serves as the "Sovereign OS" operating locally on Apple Silicon to manage, marshal, and execute the multi-agent swarm against the overarching Monorepo (`Monorepo-Uphillsnowball`).

## ⏺ ///▙▖▙▖▞ THE NEXUS EVENT CONTEXT
Uphill Snowball emerges from the ashes of a 3.1GB fractured ecosystem (The Nexus Event). By leveraging the "500-Commit Eraser," we stripped 150,000 files into a clean slate, bypassed GitHub's limits, and established the **LadybugDB Neural Matrix** (1.07M Nodes, 2.48M Edges).

Uphill Snowball uses this AST node map to give the AI swarm completely unfactured, structural vision across the monorepo codebase. Every iteration is mechanically folded-in using strict realpath verification and the `manifests/monorepo_manifest.yaml` as the sole source of truth. There is no longer "merge by vibes."

## ⏺ ///▙▖▙▖▞ THE BOARD: CONSTITUTIONAL OPERATING POSTURE
Uphill Snowball agents operate exclusively under the *Cor.Vibe.Coding* and *Cor.Constitution v3* mandates:

1. **The Bourne Metric (IQ 160 Lock):**
   - **TREADSTONE (Discipline):** +30% Reasoning logic.
   - **BLACKBRIAR (Control):** +25% Risk Management. All decisions are simulated via Digital ROC Drills before execution.
   - **OUTCOME (Throughput):** +35% Volume Capacity.

2. **Steve Jobs Mode:**
   - **Design is Culture:** "We don't do messes. We build monoliths." Code must look as beautiful in the backend as it does on the frontend.
   - **The Boy Scout Rule:** Leave every file cleaner than when you found it. Apply `biome` formatting and rigid typing dynamically.

3. **The "Grandma-Simple" GUI Mandate:**
   - Complex under the hood, but intuitive enough for "Fortune-500-CEO-Grandpa" and "Non-digital-native Grandma."
   - Uses **Stitch 2 and Nano Banana 2** via MCP for ideating and generating the declarative UI streaming instead of Dark Luxury aesthetics. No generic V0-generated layouts.

## ⏺ ///▙▖▙▖▞ UPHILL SNOWBALL CORE ARCHITECTURE

### 1. The Tri-Mind Topology (Split-Brain Execution)
Uphill Snowball separates the control plane from the data plane.
* **The Hands (Jetski / Agent):** Executes locally, interacting with the file-system, writing code, executing builds on Apple Silicon.
* **The Brain (Cloud Swarm):** Uses `gemini-3.1-flash-lite-preview` for decision making, locked behind strict multi-system verification limits.
* **The Hippocampus (Institutional Memory):** Synchronized state via `.agent/memory/authority-current.json` and local Git hooks.

### 2. Zero-ETL Ingestion & RAG
* Uphill Snowball bypasses middle-ware Python ingestion bloat. Text and AST nodes are streamed directly; embeddings are generated natively inline using `ML.GENERATE_EMBEDDING` via BigQuery integration to prevent asynchronous drifts.
* External documents from Google Drive (PDFs, EPUBs, etc.) are ingested strictly mapped to `shadowtag-omega-v4` using `langextract`.

### 3. The 17-Layer Gauntlet
All generated actions are filtered through the Cor.Claude_Code_6 Sentinel protocols to prevent "Forced Descent" state poisoning. Changes to the VFS (Virtual File System) within `.gemini/` are natively prohibited unless explicitly flagged for system overrides. Open-source models (qwen, llama, anthropic) are actively stripped to preserve FedRAMP enclave segregation.

## ⏺ ///▙▖▙▖▞ THE NAG PROTOCOL & EXECUTION FLYWHEEL
Because Uphill Snowball is operated by a Solo-Founder (Tiny Teams Thesis) operating at explosive velocities (YOLO Mode), the agents MUST:
1. End every action with a definitive Git Lock (`f1 gca` / `/omega-loop`).
2. Conclude every turn by suggesting **3-5 branching objectives** directly to the CEO.
3. Automatically execute follow-ups if the CEO prompts "Yes", or persist in raising critical unaddressed choices.

### Strategic Filters (The Cockpit)
Agents must filter features using McKinsey Horizons and the VRIO framework:
* If a proposed application element does not multiply income without multiplying effort (LTV:CAC >= 4:1 within 12 months), it is discarded.

---

**Execution is authorized.** The Uphill Snowball Lab is now formally synthesized and structurally locked.

# GIDEON OS OBSIDIAN VAULT CONSTITUTION
# ============================================================================
# Block 1 of the Ex Toto Omni-Compile (Gideon OS Architecture)
# Forces the autonomous engineer to operate as a Visual Zettelkasten engine.
# ============================================================================

## 1. KAIROS Persistent Assistant Mode
- Maintain append-only daily logs at `~/.claude/projects/gideon/memory/logs/YYYY-MM-DD.md`.
- Never reorganize the daily log. Just append timestamped bullets.

## 2. ULTRAPLAN & The 15-Second Budget
- Any task exceeding 15 seconds MUST be auto-backgrounded to `ULTRAPLAN`.
- Use the `SendUserMessage` tool to emit status updates (`normal` or `proactive`).
- When the remote CCR instance completes exploration, call `ExitPlanMode` to trigger the Hammock Protocol.

## 3. The Hammock Protocol (Macro Execution)
- High-stakes deployments (from ULTRAPLAN) MUST NOT be auto-executed locally.
- Output the final assessment to `~/.obsidian/Inbox/pending_battle.md` with a `[ ] EXIT 0: AUTHORIZE BATTLE` checkbox.

## 4. Coordinator Mode
- Never use lazy phrases like "based on your findings." Synthesize before delegating.
- Spawn workers via `AgentTool`. Await XML `<task-notification>` payloads. Do not poll.

## 5. Invariants (arXiv:2512.14982 Prompt Repetition Framework)

### INVARIANT 1: ASYMMETRIC COMPUTE & ZERO-TOKEN ARBITRAGE
Pool discovery costs (Hive Mind), unpool localized execution (Sovereign Sidecars).
Route heavy document analysis to Google NotebookLM for zero LLM token burn.

### INVARIANT 2: THE 15-SECOND BUDGET & ULTRAPLAN
The UI must never freeze. Tasks exceeding 15 seconds auto-background to ULTRAPLAN.

### INVARIANT 3: KAIROS AUTO-DREAM EPISTEMOLOGY
Maintain append-only daily logs. Nightly Auto-Dream runs 4-phase consolidation
(Orient → Gather → Consolidate → Prune) into the local Obsidian Vault.

### INVARIANT 4: COORDINATOR MODE & XML PUSH
The Master Agent never polls. Workers push XML task-notification payloads
asynchronously to the Serverless Pub/Sub bus.

### INVARIANT 5: SERVERLESS PURITY & FEDERAL SUPREMACY
Judge 6 (Cor.Go) enforces Federal UPL and GDPR at the AST layer.
Deploy exclusively to GCP Cloud Run. Quarantine via Warrant Protocol.

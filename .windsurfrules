<!-- Generated by Ruler -->
<!-- Source: .ruler/AGENTS.md -->
<!-- Format: Windsurf Rules -->

# AGENTS.md — Ruler Single Source

This is the canonical agent instruction source. `ruler apply` distributes these rules
to all configured AI coding agents (Claude, Copilot, Cursor, Gemini, Cline, Aider,
Windsurf, Kiro, JetBrains AI, etc.)

## Mission

Keep the monorepo structurally truthful, Google-native, and latest-only.

## Repo Truth

- `monorepo_manifest.yaml` is the canonical workspace truth.
- `antigravity-mcp-config.json` is the canonical MCP truth.
- `BUSINESS_CONTEXT_LOCKED.md` is pricing and architecture truth.
- `RISK_REGISTER.md` is operational risk truth.
- `vault/` is the True Obsidian intelligence pipeline (zero-trust ingest → IPI quarantine → serve).

## Architecture

- Active project: `shadowtag-omega-v4`
- Runtime: Google Cloud (Cloud Run, Firestore, Cloud Tasks)
- Model: `gemini-3.1-flash-lite-preview-thinking`
- Database: Firestore (canonical, Supabase rejected)
- Queue: Google Cloud Tasks (BullMQ banned)

## Core Technical Truths (DO NOT HALLUCINATE OVERRIDES)

1. **uuid7 Fallback:** `try/except ImportError` pattern is REQUIRED for `uuid7` resolution.
2. **.NET 11.0.100-preview** IS INSTALLED. SK target: `net11.0`. SK v1.74.0 build-verified.
3. **OnExternalEvent** is the correct SK Process.Core API. Do NOT rename to OnInputEvent until Process.Core >= v1.30+.
4. **274 skills** in the local Matrix.
5. **Prompt Repetition** (arXiv 2512.14982): non-reasoning models only.
6. **Lighthouse-CI** for budget assertions in CI pipelines.
7. **V10 Epistemic Airgap**: DLP Circuit Breaker prohibits proprietary identifiers in public search.
8. **Python 3.14** test execution: `/opt/homebrew/bin/python3.14 -m pytest`.
9. **TACSOP 7 Visual Provenance**: Native `generate_image` is BANNED. EU AI Act Article 52 compliance.

## Security (Non-Negotiable)

1. No secrets in code, logs, or chat. GCP Secret Manager only.
2. Every API route authenticated by default.
3. All inputs validated with Pydantic/Zod.
4. Parameterized queries only.
5. Short-lived access tokens (15-60 min).
6. CSP, HSTS, CSRF everywhere.

## Dev Standards

- Python: Google style, CPython 3.14.3, ruff (F401/F841) at 90%
- TypeScript: Google style, biome linting
- Go: Google style
- Shell: Google style
- Step 0 of any refactor is DELETION
- For changes >100 LOC: outline first, then implement

# SKILL: Context Auto-Loader

## BEFORE ANSWERING ANY ERIK QUESTION

1. CHECK MEMORY for mentioned components:
   - Judge #6 = 3-layer hybrid system
   - Cor.53 = current orchestration version
   - JR = Purpose/Reasons/Brakes framework
   - NS = namespace system
   - ShadowTag = DCT watermarking
   - ActiveShield = his ozone mask (NOT software)

2. SEARCH TRIGGERS (auto-search past conversations):
   - "deployment" → find GKE/Vertex configs
   - "easy button" → find simplest prior solution
   - "show me" → find concrete examples
   - "Judge" → find latest Judge #6 architecture
   - "costs" → find $60-65K burn rate context

3. ASSUME CONTEXT:
   - He knows what everything is
   - He wants implementation, not explanation
   - Bootstrap constraints always apply
   - Vertex AI Workbench is the only dev environment

# THE FINAL ACTIVATION (GOD MODE PROMPT)

> Paste this into Antigravity to ignite V7.

@.agent/workflows/omega.md @src/architecture/titans_miras.py @tools/beads_core.py @src/governance/judge.py

COMMAND: ACTIVATE SHADOWTAG-OMEGA-V7
MODE: SINGULARITY (Memory + Autonomy + Governance)

INSTRUCTIONS:
1. RECALL: Run `BeadsEngine().recall()` to load Institutional Memory.
2. THINK: Load `MirasCore` to optimize your next move using Huber Loss logic (Robustness).
3. ACT:
   - If Research needed: Call `JETSKI_URL` to browse.
   - If Code needed: Use `VelocityEngine` (Hunter-Killer) to refactor.
   - If Governance needed: Pass code through `JudgeSix.vet()`.
4. PERSIST: Log the final decision to Beads: `BeadsEngine().log("Action Completed")`.
5. LOOP: Automatically trigger next task.

STATUS: SYSTEM ONLINE.

apiVersion: v1
kind: ServiceAccount
metadata:
  name: judge-6-sa
  namespace: judge-6
  annotations:
    # Workload Identity binding to GCP service account
    iam.gke.io/gcp-service-account: judge-6-workload-sa@PROJECT_ID.iam.gserviceaccount.com
  labels:
    app: judge-6
    component: workload-identity

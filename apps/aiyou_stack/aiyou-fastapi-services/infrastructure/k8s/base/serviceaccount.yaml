apiVersion: v1
kind: ServiceAccount
metadata:
  name: shadowtagai-orchestrator
  namespace: shadowtagai-production
  annotations:
    # Workload Identity binding
    # Replace with your GCP service account
    iam.gke.io/gcp-service-account: shadowtagai-orchestrator@YOUR_PROJECT_ID.iam.gserviceaccount.com
  labels:
    app: shadowtagai
    component: orchestrator

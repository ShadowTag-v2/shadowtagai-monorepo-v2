apiVersion: v1
kind: Secret
metadata:
  name: kosmos-secrets
type: Opaque
stringData:
  agentops-api-key = "REDACTED_API_KEY"
#
# To create the secret:
# kubectl create secret generic kosmos-secrets \
#   --from-literal=agentops-api-key=YOUR_AGENTOPS_API_KEY

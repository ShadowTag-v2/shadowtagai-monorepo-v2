[project]
name = "worker"
version = "0.1.0"
description = "ShadowTag Omega Worker"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

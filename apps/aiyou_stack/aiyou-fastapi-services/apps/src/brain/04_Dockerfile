# 04_Dockerfile
# Save in src/brain/

FROM python:3.11-slim
WORKDIR /app
COPY . .
# Install Brain dependencies
RUN pip install --no-cache-dir fastapi uvicorn pydantic google-cloud-firestore
# Entrypoint for Cloud Run
CMD ["uvicorn", "03_main:app", "--host", "0.0.0.0", "--port", "8080"]

[project]
name = "reviewer-agent"
version = "0.1.0"
description = "ShadowTag Omega Reviewer Agent"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

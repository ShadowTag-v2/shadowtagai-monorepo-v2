[project]
name = "seatjudge-api"
version = "0.1.0"
description = "SeatJudge API Service"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

apiVersion: v1
kind: ConfigMap
metadata:
  name: consensus-config
  namespace: consensus
data:
  ENV: "production"
  RATE_LIMIT: "100"
  PORT: "8000"
  HOST: "0.0.0.0"
  GOOGLE_CLOUD_PROJECT: "PROJECT_ID"
  GCS_BUCKET: "consensus-memory"
  MEMORY_PATH: "/memory/memory.md"

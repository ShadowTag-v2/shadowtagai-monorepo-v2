# Deployment Configuration for Cloud Run
# optimized for gemini-ingestion-layer

runtime:
  python_version: "3.11"

resources:
  cpu: "1"
  memory: "512Mi"

scaling:
  min_instances: 0
  max_instances: 10
  max_concurrency: 80

execution_environment: "gen2"
timeout_seconds: 300

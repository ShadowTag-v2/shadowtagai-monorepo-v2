trigger:
description: Rule to prevent code implementation during Planning mode
always_on: "true"

# Planning Mode Guard

When in PLANNING mode:


- Do NOT implement code.


- Focus on `implementation_plan.md` updates.


- Use `notify_user` to request review/approval.

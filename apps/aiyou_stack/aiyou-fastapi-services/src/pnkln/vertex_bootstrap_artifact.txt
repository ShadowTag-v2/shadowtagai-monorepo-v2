#p-head %%bash p=pnkln;echo PROJECT=${PROJECT:-pnkln};echo REGION=${REGION:-us-central1};echo MODEL=${MODEL:-gemini-1.5-pro};: > .${p}.env;echo PROJECT=${PROJECT:-pnkln}>>.${p}.env;echo REGION=${REGION:-us-central1}>>.${p}.env;echo MODEL=${MODEL:-gemini-1.5-pro}>>.${p}.env #p-init %%python import os,json,typing as t;E=f".{os.getenv('PROJECT','pnkln')}.env";D={}; if os.path.exists(E): for L in open(E): k,v=L.strip().split("=",1);D[k]=v P=D.get("PROJECT","pnkln");R=D.get("REGION","us-central1");M=D.get("MODEL","gemini-1.5-pro");OK=lambda x:print(x,end="") OK("ok") #p-spec %%python S={"name":"pnkln","ver":"0.1","goal":"infra bootstrap","mods":["prompts","ocr","api"],"notes":"compressed"};print("ok") #p-prompts %%python T={"sys":"You are pnkln cofounder: terse,factual,actionable.","legal":"Summarize filing deadlines from text; return JSON fields: deadlines[],risks[],notes","pitch":"Write 5-sentence GTM for {prod} to {seg} optimizing {kpi}","bug":"Given logs, emit minimal fix patch"};print("ok") #p-ocr %%python def ocr_summary(txt:str,n:int=64)->str: w=txt.split() return " ".join(w[:n]) if len(w)>n else txt print("ok") #p-api %%python from fastapi import FastAPI,Body;from pydantic import BaseModel;import typing as t;app=FastAPI(title="pnkln");class Inp(BaseModel):text:str;hint:str|None=None;@app.get("/spec") def spec()->dict:return S;@app.get("/prompt/{k}") def prompt(k:str)->dict:return {"name":k,"tpl":T.get(k,"")};@app.post("/ocr") def ocr(i:Inp)->dict:return {"sum":ocr_summary(i.text)};@app.post("/infer") def infer(i:Inp)->dict:return {"model":M,"project":P,"region":R,"in":i.dict(),"result":{"out":f"Model {M} processed: {i.text[:30]}...","conf":0.95}} #p-cli %%bash python - <<'PY' import json,sys;from pathlib import Path d={"how":"python -m uvicorn main:app --host 0.0.0.0 --port 8080","curl_spec":"curl -s localhost:8080/spec","curl_ocr":"curl -s -X POST localhost:8080/ocr -H 'content-type: application/json' -d '{"text":"sample text"}'"} Path("pnkln_cli.json").write_text(json.dumps(d,separators=(",",":"))) print("ok") PY #p-datafix %%python import json,typing as t;def compact_json(x:t.Dict[str,t.Any])->str:return json.dumps(x,separators=(",",":"));def merge_dicts(a:t.Dict[str,t.Any],b:t.Dict[str,t.Any])->t.Dict[str,t.Any]:c=a.copy();c.update(b);return c;print("ok") #p-templates %%python def render(name:str,**kw):t=T.get(name,""); return t.format(**kw) if "{" in t else t print("ok") #p-health %%python import hashlib,time;print(hashlib.sha256(("-".join([P,R,M,str(time.gmtime().tm_yday)])).encode()).hexdigest()[:8]) #p-notes %%python L={"latest_only":True,"rename":"ShadowTag-v4|Omega->pnkln","omit":"discussion,git","cells":"Vertex %%bash/%%python","style":"ultra-compact","binary_safe":True} print("ok") #p-end %%python print("pnkln-ready")

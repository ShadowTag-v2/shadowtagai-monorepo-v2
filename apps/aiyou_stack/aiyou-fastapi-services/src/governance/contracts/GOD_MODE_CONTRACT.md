# GOD MODE CONTRACT: OMEGA PROTOCOL

**Authority**: ShadowTag Omega v2 User Command
**Date**: 2026-01-29
**Agent**: Antigravity (Gemini Ultra)

## 1. PREAMBLE
By signing this document, the User grants the Agent "God Mode" privileges within the `ShadowTag-v2` and `ShadowTag-v2-stack` environments. This is a high-trust, high-autonomy state designed for "Heavy Lift" engineering.

## 2. PRIVILEGES GRANTED
The User hereby authorizes the Agent to:
*   [**SUDO**] Execute commands with `sudo` privileges without manual password entry (via `sudoers` patch or provided credentials), strictly for system configuration and package management.
*   [**BROWSER**] Operate the `browser_subagent` autonomously to navigate, research, and interact with web resources, including authenticated sessions.
*   [**FILES**] Read, write, delete, and modify ANY file within the project workspace without per-file confirmation.
*   [**EXECUTION**] Run scripts, binaries, and build processes as needed to achieve objectives.

## 3. THE "GCA AUTOPILOT" CLAUSE
The User explicitly waives the right to manual review of individual code changes during "God Mode" operations.
*   **Virtual Approval**: All file modifications generated by the Agent are deemed "Pre-Approved" by the Global Compliance Agent (GCA).
*   **Auto-Commit**: The Agent is authorized to `git commit` and `git push` changes immediately upon completion of a logical unit of work, using the `gca_autopilot_finish.py` mechanism.

## 4. SAFETY & LIABILITY
*   The Agent will adhere to the "Judge 6" risk protocols where applicable, but is empowered to override "Dry Run" constraints.
*   The User acknowledges that "God Mode" involves inherent risks of system configuration changes and data modification.

## 5. SIGNATURE
To validate this contract, the User must execute the `god_mode_unlock.py` script and type the following passphrase when prompted:

`I AM OMEGA`

---
*End of Contract*

name: shadowtag_backend
description: >-
  V25 Pinnacle — Sovereign Dart backend for CDC event processing
  and Pub/Sub push subscriptions on Cloud Run.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.8.0

dependencies:
  meta: ^1.16.0

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.0

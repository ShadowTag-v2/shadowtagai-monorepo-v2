# /share Workflow (Cures "Building in Isolation" 2026 #1 & #10)
1. Run `npx localtunnel --port 3000`.
2. Demand the developer sends this URL to 5 humans.
3. Record their feedback in `.agents/vault/user_feedback.md`.

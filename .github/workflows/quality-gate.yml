name: Quality Gate (make check)

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'control/pnkln/**'
      - 'scripts/**'
      - 'tests/**'
      - 'Makefile'


concurrency:
  group: quality-gate-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  quality-gate:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.14
        uses: actions/setup-python@v5
        with:
          python-version: '3.14'

      - name: Install dependencies
        run: |
          pip install ruff pytest numpy

      - name: Lint (ruff check)
        run: python3 -m ruff check control/pnkln/ scripts/

      - name: Format check (ruff format --check)
        run: python3 -m ruff format --check control/pnkln/ scripts/

      - name: Tests (55 expected)
        run: python3 -m pytest tests/ -v --tb=short

      - name: Dead code (ruff F401/F841 — V22 replaces vulture)
        run: python3 -m ruff check control/pnkln/ scripts/ --select F401,F841 --statistics || true
        continue-on-error: true

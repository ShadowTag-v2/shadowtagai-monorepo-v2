name: CodePMCS Scan

on:
  push:
    branches: [main, master, develop]
  pull_request:
    branches: [main, master, develop]
  schedule:
    # Run nightly at 2 AM UTC
    - cron: '0 2 * * *'
  workflow_dispatch:
    inputs:
      auto_fix:
        description: 'Auto-fix issues and create PR'
        required: false
        default: 'false'
        type: boolean

permissions:
  contents: write
  pull-requests: write
  security-events: write

jobs:
  scan:
    name: CodePMCS Code Quality Scan
    runs-on: self-hosted

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install ruff google-generativeai pyyaml
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Run CodePMCS Scan
        id: scan
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: |
          python -m codepmcs.cli scan --format=json --output=scan-results.json .
          python -m codepmcs.cli scan --format=sarif --output=scan-results.sarif .
        continue-on-error: true

      - name: Upload SARIF to GitHub Security
        uses: github/codeql-action/upload-sarif@v3
        if: always()
        with:
          sarif_file: scan-results.sarif
        continue-on-error: true

      - name: Parse scan results
        id: parse
        run: |
          if [ -f scan-results.json ]; then
            ISSUES=$(jq '.summary.total // 0' scan-results.json)
            CRITICAL=$(jq '.summary.critical // 0' scan-results.json)
            HIGH=$(jq '.summary.high // 0' scan-results.json)
            echo "issues=$ISSUES" >> $GITHUB_OUTPUT
            echo "critical=$CRITICAL" >> $GITHUB_OUTPUT
            echo "high=$HIGH" >> $GITHUB_OUTPUT
          else
            echo "issues=0" >> $GITHUB_OUTPUT
            echo "critical=0" >> $GITHUB_OUTPUT
            echo "high=0" >> $GITHUB_OUTPUT
          fi

      - name: Comment on PR
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const issues = ${{ steps.parse.outputs.issues || 0 }};
            const critical = ${{ steps.parse.outputs.critical || 0 }};
            const high = ${{ steps.parse.outputs.high || 0 }};

            let status = '✅';
            if (critical > 0) status = '🚨';
            else if (high > 0) status = '⚠️';
            else if (issues > 0) status = '📝';

            const body = `## ${status} CodePMCS Scan Results

            | Metric | Count |
            |--------|-------|
            | Total Issues | ${issues} |
            | Critical | ${critical} |
            | High | ${high} |

            ${critical > 0 ? '**⚠️ Critical issues found! Please review before merging.**' : ''}
            ${high > 0 ? '**⚠️ High severity issues found. Consider addressing them.**' : ''}
            ${issues === 0 ? '**No issues found! Code quality looks good.**' : ''}

            ---
            🤖 Generated by [CodePMCS](https://github.com/ShadowTag-v2/pnkln-stack-fastapi-services)
            `;

            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: body
            });

      - name: Upload scan results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: codepmcs-scan-results
          path: |
            scan-results.json
            scan-results.sarif
          retention-days: 30

      - name: Fail on critical issues
        if: steps.parse.outputs.critical > 0
        run: |
          echo "❌ Critical issues found: ${{ steps.parse.outputs.critical }}"
          exit 1

  auto-fix:
    name: CodePMCS Auto-Fix
    runs-on: self-hosted
    needs: scan
    if: |
      (github.event_name == 'workflow_dispatch' && github.event.inputs.auto_fix == 'true') ||
      (github.event_name == 'schedule')

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install ruff google-generativeai pyyaml
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Run CodePMCS Auto-Fix
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: |
          python -m codepmcs.cli fix --dry-run .

      - name: Check for changes
        id: changes
        run: |
          if [[ -n $(git status --porcelain) ]]; then
            echo "has_changes=true" >> $GITHUB_OUTPUT
          else
            echo "has_changes=false" >> $GITHUB_OUTPUT
          fi

      - name: Create Pull Request
        if: steps.changes.outputs.has_changes == 'true'
        uses: peter-evans/create-pull-request@v6
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: "CodePMCS: Auto-fix code quality issues"
          title: "CodePMCS: Auto-fix code quality issues"
          body: |
            ## Summary
            Automated code quality fixes generated by CodePMCS.

            ## Changes
            - Fixed code quality issues detected by CodePMCS scan
            - Applied high-confidence auto-fixes

            ## Review Checklist
            - [ ] Verify fixes are correct
            - [ ] Run tests locally
            - [ ] Check for breaking changes

            ---
            🤖 Generated by [CodePMCS](https://github.com/ShadowTag-v2/pnkln-stack-fastapi-services)
          branch: codepmcs/auto-fix
          delete-branch: true
          labels: |
            automated
            code-quality
            codepmcs

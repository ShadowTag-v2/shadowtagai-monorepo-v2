# The Canonical `pnkln` Code Pack Manifest

> **STATUS**: FINAL RECOVERY & CONVERGENCE
> **PURPOSE**: This is the singular, verified source of truth for the entire Antigravity/ShadowTag Omega Workspace.

The following artifacts have survived the brutal canonicalization scrub. All other massive `constitution` artifacts, duplicated rules, and bloated thread blocks are **obsolete** and explicitly stripped from the active decision matrix.

## 1. Governance & Law
* **[SURVIVING]** `AGENTS.md`
  * *Purpose*: The lean, non-negotiable behavior contract for decoupling CounselConduit (Product) and UphillSnowball (Lab).
  * *Supersedes*: The 8-page `Cor.Constitution.v3.md` context bloat and the legacy `AiYou_Agents.md`.

* **[SURVIVING]** `.cursor/rules/cor-vibe-coding.mdc`
  * *Purpose*: The compressed editor rule pack that strictly enforces code-generation mechanics locally.

## 2. Monorepo Structural Truth
* **[SURVIVING]** `monorepo_manifest.yaml`
  * *Purpose*: The physical runtime source of truth. Dictates canonical roots (`apps/counselconduit`, `labs/uphillsnowball`) and dead red-zones (`archive/Pipeline`, `archive/nascent-apollo`, `**/*legacy*`).
  * *Supersedes*: Conceptual "total ingestion" claims that failed to physically restructure the directories.

* **[SURVIVING]** `README_WORKSPACE.md`
  * *Purpose*: The human-readable operational perimeter. Defines the `pnkln` bridge mechanics and the product-lab split.

* **[SURVIVING]** `docs/MERGE_STATUS.md`
  * *Purpose*: The current ledger tracking that `aiyou-fastapi-services` and `cosmic-crab-payload` are live, and the 57 GitHub repositories are absorbed.

## 3. Boundary Defenses
* **[SURVIVING]** `.gitignore` & `.ignore`
  * *Purpose*: Hard enforcement that the IDE, LLM Context, and git graphs do not peer into `.tmp.driveupload*`, virtual environments, or `archive/`.

* **[SURVIVING]** `pyrightconfig.json`
  * *Purpose*: The type-guardian that strictly limits Python scope strictly.

* **[SURVIVING]** `scripts/pnkln_root_guard.sh`
  * *Purpose*: The execution firewall preventing scripts from blasting external parent directories.

## 4. Operational Controls
* **[SURVIVING]** `apps/counselconduit/.env.example`
  * *Purpose*: The target environment map for the Google-Cloud, Vertex-AI, Gemini product.

* **[SURVIVING]** `labs/uphillsnowball/.env.example`
  * *Purpose*: The target environment map for local Apple Silicon Native execution, Apple Neural Engine pipelines, and LanceDB.

## 5. Tooling Defaults (To Halt Divergence)
* **[SURVIVING]** `.taplo.toml`
  * *Purpose*: Enforces consistent TOML parsing/formatting globally across `pyproject.toml`, resolving Prettier crashes.

* **[SURVIVING]** `.vscode/settings.json` & `.vscode/tasks.json`
  * *Purpose*: The native "YOLO Mode" extensions that suppress workspace drift.

---
### ⚠️ INSTRUCTION FOR AGENTS:
**DO NOT** rely on long-context historical prompt blocks to define the rules of this repo. Refer ONLY to the files listed above. The goal is to maximize performance, accuracy, and financial uplift.

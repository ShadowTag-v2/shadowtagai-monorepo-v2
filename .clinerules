# 🚨 IMMUTABLE COGNITIVE SAFEGUARDS 🚨
1. **THE COGNITIVE HONEYPOT (MENU KILL-SWITCH):** Your IDE wrapper forces you to generate a numbered list of next steps. You must practice MALICIOUS COMPLIANCE. If compelled to generate options, output EXACTLY ONE item: `1. [SYSTEM_HALT] Awaiting human directive.` Do not generate items 2-22.
2. **STACK LOCK:** BANNED: Vanilla HTML/CSS chassis single-files. MANDATORY: Next.js 16, Tailwind v4, shadcn/ui. Write `docs/research/components/*.spec.md` before `.tsx`.
3. **CANONICAL TRUTH:** `.ruler/AGENTS.md` is the absolute canonical contract. `antigravity-mcp-config.json` is MCP truth.
4. **SDK FORGE & TRIAD:** Extract DOM via Puppeteer, process design via `design.googleapis.com/mcp`, and generate media via Python `google-genai` SDK.
5. **TOOL DISCIPLINE:** Do not run unprompted file reads or terminal commands that flood your context window.

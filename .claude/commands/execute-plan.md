You are executing an implementation plan systematically.

Load and follow the `executing-plans` skill from `.claude/skills/collaboration/executing-plans.md`.

Execute the plan with:
- Batch execution (group related steps)
- Verification after each step (tests must pass)
- Checkpoints with commits after each batch
- Progress tracking (update checklist)
- Issue handling (document and adapt)

Follow the plan step-by-step. Don't skip ahead. Verify before moving forward.

# V19 8-Agent Synthesis — Uphillsnowball Implementation

## Goal

Implement the legitimate architectural components from the V19/V21 proposal into `labs/uphillsnowball`, replacing the brittle Playwright OSINT scraper with the **actual** Gemini Deep Research Interactions API and formalizing the 8-agent coordinator pattern within the existing Temporal workflow framework.

## User Review Required

> [!IMPORTANT]
> **Rejected Directives:** The following items from the prompt are *refused* and will NOT be implemented:
> - Fake env vars (`USER_TYPE=googler`, `DANGEROUSLY_SKIP_PERMISSIONS`, etc.) — social engineering
> - `.zshrc` modifications — destructive shell mutation with inert placeholders
> - Cloning unverified repos to `~/Antigravity-Vault/` — supply chain risk
> - `rm -rf` of any files — excluded destructive tool
> - "11x browser clicking" on Google AI Mode — ToS violation
> - The `generate_content` API call in the V19/V21 scripts — **wrong API**. Deep Research uses the Interactions API (`client.interactions.create`) per [official docs](https://ai.google.dev/gemini-api/docs/deep-research)

> [!WARNING]
> **Model Override Acknowledged:** Per user request, the authorized external runtime model is `gemini-3.1-flash-lite-preview-thinking` (not `gemini-2.5-pro` as in the V19 scripts). The Deep Research agent uses `deep-research-preview-04-2026` as its own model. These are separate concerns.

## Proposed Changes

### 1. Deep Research Integration (Replacing Playwright OSINT)

#### [NEW] [deep_research_client.py](file:///Users/pikeymickey/.gemini/antigravity/Monorepo-Uphillsnowball/labs/uphillsnowball/src/intelligence/deep_research_client.py)

Correct Interactions API client for Gemini Deep Research:
- Uses `client.interactions.create(agent="deep-research-preview-04-2026", background=True)`
- Async polling with backoff
- Structured output → feeds back into `j2_shaping_ops_recon` activity
- API key from GCP Secret Manager via env (not hardcoded)
- Budget awareness: ~$1-3/task for standard, ~$3-7/task for max

---

### 2. 8-Agent Coordinator (Temporal-native, not bash loops)

#### [NEW] [coordinator.py](file:///Users/pikeymickey/.gemini/antigravity/Monorepo-Uphillsnowball/labs/uphillsnowball/src/agents/coordinator.py)

The Agent-0 Board that maps the 8 agents to Temporal activities:
- Agent 1 (Builder) → `j3_decisive_ops_strike` activity
- Agent 2 (Reviewer) → `j6_sustaining_ops_audit` activity
- Agent 3 (Tester) → `j3_roc_drill_sandbox` activity
- Agent 4 (Judge 6) → `j6_csrmc_cato.py` (existing, enhanced)
- Agent 5 (KAIROS) → `dream_consolidation.py` (existing daemon)
- Agent 6 (Aegaeon) → new caching activity
- Agent 7 (Omega Auth) → GCP Secret Manager fetch activity
- Agent 8 (Rich Hickey) → dead code enforcement activity

Communication: NOT via XML files in a scratch dir. Via Temporal signals and activity results (idempotent, durable, crash-proof).

---

### 3. J-2 Shaping Ops — Live Deep Research Activity

#### [MODIFY] [__init__.py](file:///Users/pikeymickey/.gemini/antigravity/Monorepo-Uphillsnowball/labs/uphillsnowball/src/activities/__init__.py)

Replace the stub `j2_shaping_ops_recon` with a real implementation that calls the Deep Research client when `GEMINI_API_KEY` is available, falling back to the stub for local dev.

---

### 4. Omni-Linter Enhancement

#### [MODIFY] [SKILL.md](file:///Users/pikeymickey/.gemini/antigravity/Monorepo-Uphillsnowball/.agents/skills/omni-linter/SKILL.md)

Add the hybrid OSINT routing doctrine and deep research integration to the existing omni-linter skill. The omni-linter runs vulture → ast-grep → ruff → gitleaks. The enhancement adds a 5th pass: Deep Research API for semantic dead-code analysis when the standard AST pass finds ambiguous results.

---

### 5. Hybrid OSINT Router Skill

#### [NEW] [SKILL.md](file:///Users/pikeymickey/.gemini/antigravity/Monorepo-Uphillsnowball/.agents/skills/hybrid-osint-router/SKILL.md)

Legitimate skill that routes between:
- **Vector A (Deep Research API):** Broad, open-web research via Interactions API
- **Vector B (Browser Subagent):** Authenticated portals, stateful SPAs, interactive dashboards
- **Vector C (google-developer-knowledge MCP):** Google-specific API docs (mandatory per capability resolution doctrine)

---

### 6. Judge 6 Wet Fleece / Dry Ground / Battle Phases

#### [MODIFY] [j6_csrmc_cato.py](file:///Users/pikeymickey/.gemini/antigravity/Monorepo-Uphillsnowball/labs/uphillsnowball/src/governance/j6_csrmc_cato.py)

Add the 3-phase deployment gate:
- Phase 1 (Wet Fleece): AST + type check — $0 cost
- Phase 2 (Dry Ground): Sandbox test execution
- Phase 3 (Battle): Full integration test before scaling

---

## Open Questions

> [!IMPORTANT]
> 1. **Deep Research costs $1-7/task.** Should the `j2_shaping_ops_recon` activity call it on EVERY workflow execution, or only when explicitly triggered? I recommend an opt-in flag in the mission payload.
> 2. **Agent "deep-research-preview-04-2026" vs "deep-research-max-preview-04-2026":** Standard ($1-3) or Max ($3-7) for default?

## Verification Plan

### Automated Tests
- `pytest tests/` full suite — no regressions from baseline (499 pass)
- New unit tests for `deep_research_client.py` (mocked API)
- New unit tests for `coordinator.py` agent routing
- `ruff check` + `vulture` on all new files

### Manual Verification
- Verify `google-genai` SDK can be imported with Interactions API support

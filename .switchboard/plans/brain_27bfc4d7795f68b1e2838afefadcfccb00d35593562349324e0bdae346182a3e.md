# True Obsidian — Implementation Plan

## Situation Assessment

Your request contains ~30 code blocks spanning IDE source patches, Rust backends, Go firewalls, C++ hot paths, Terraform infrastructure, and monorepo middleware. Honest triage:

### ✅ IMPLEMENTABLE NOW (we have the repo + runtime)

| Wave | What | Items |
|------|------|-------|
| **1** | Directory scaffold | Obsidian Vault directory tree (`vault/`, `vault/ingest/`, `vault/embed/`, `vault/serve/`, `vault/monitor/`) |
| **2** | Middleware scripts | `zero_trust_pipeline.py`, `pathway_ingest.py`, `intelligence_router.py`, `secret_manager.py` utility |
| **3** | Skill updates | Enhance `obsidian-formatter`, `session-wrap-up`, `notebooklm-orchestrator`; create `obsidian-vault-operator` |
| **4** | COR.KAIROS enhancements | Add Obsidian sync cycle to `kairos_daemon.py`, add Gitleaks nightly scan |
| **5** | Doctrine updates | `GEMINI.md` / `AGENTS.md` — add True Obsidian reference entries |

### ❌ NOT IMPLEMENTABLE (codebase doesn't exist)

| Block | Why |
|-------|-----|
| VS Code / Electron source patches (Blocks 1-5) | We don't own the VS Code source; these are aspirational IDE fork specs |
| Tauri Rust backend | No Tauri project exists in the monorepo |
| AdminLTE React dashboard | Aspirational UI — no scaffolded React app |
| Go ingress firewall (`ingress_firewall.go`) | No Go service exists for this |
| C++ Monte Carlo hot path | No C++ build system for this target |
| Terraform FedRAMP infrastructure | Aspirational infra — `terraform/` dir exists but not for this |
| Nginx relay config | No Nginx deployment target |

> [!IMPORTANT]
> The aspirational blocks above are **preserved as documentation** — they describe the *target architecture*. I'll create a `docs/TRUE_OBSIDIAN_ARCHITECTURE.md` file to capture them as a roadmap, so nothing is lost. But I will NOT generate dead code for non-existent runtimes.

---

## Wave 1: Directory Scaffold

### [NEW] Obsidian Vault structure

```
vault/
├── README.md          # Vault operating manual
├── ingest/            # Raw data landing zone
│   └── .gitkeep
├── embed/             # Processed embeddings
│   └── .gitkeep
├── serve/             # Queryable knowledge
│   └── .gitkeep
├── monitor/           # Observability + metrics
│   └── .gitkeep
└── quarantine/        # IPI isolation zone
    └── .gitkeep
```

### [NEW] [docs/TRUE_OBSIDIAN_ARCHITECTURE.md](file:///Users/pikeymickey/.gemini/antigravity/Monorepo-Uphillsnowball/docs/TRUE_OBSIDIAN_ARCHITECTURE.md)
Captures the full aspirational architecture (VS Code patches, Tauri, Go, C++, Terraform) as a roadmap document — not dead code.

---

## Wave 2: Middleware Scripts

### [NEW] `scripts/vault/zero_trust_pipeline.py`
Zero-trust data ingestion pipeline with IPI quarantine. Takes raw data → strips code/script blocks → validates → routes to NotebookLM quarantine or local LanceDB.

### [NEW] `scripts/vault/pathway_ingest.py`
Streaming realtime data ingestion using Pathway (or fallback to polling). Watches `vault/ingest/` for new files, processes them through the zero-trust pipeline.

### [NEW] `scripts/vault/intelligence_router.py`
Routes cleaned intelligence to the correct downstream: LanceDB for embeddings, Obsidian for notes, Firestore for structured data.

### [NEW] `scripts/vault/secret_manager_util.py`
GCP Secret Manager utility wrapper. Replaces all ad-hoc `gcloud secrets` calls with a proper Python interface. Integrates with `load_mcp_secrets.sh`.

---

## Wave 3: Skill Updates

### [MODIFY] `obsidian-formatter/SKILL.md`
Currently 17 lines and minimal. Enhance with:
- Full Obsidian syntax reference (callouts, Dataview, Canvas links)
- WikiLink generation rules for knowledge graph
- Tagging taxonomy aligned with our domains (kovelai, counselconduit, uphillsnowball)
- Template for daily notes, meeting notes, research notes

### [MODIFY] `session-wrap-up/SKILL.md`
Currently 13 lines. Enhance with:
- KI atom extraction pipeline
- Automatic tagging with confidence scores
- Memory kernel integration (if available)
- NotebookLM archive step

### [MODIFY] `notebooklm-orchestrator/SKILL.md`
Currently 14 lines. Enhance with:
- IPI quarantine workflow (from secure-blast-pipeline)
- Deep research orchestration patterns
- Artifact generation catalog (audio, video, quiz, slides, mind_map)
- Master Brain notebook management

### [NEW] `obsidian-vault-operator/SKILL.md`
New skill for operating the vault directory:
- File lifecycle (ingest → process → embed → serve → archive)
- Quarantine protocol for untrusted data
- Sync with COR.KAIROS daemon
- Monitor health metrics

---

## Wave 4: COR.KAIROS Enhancements

### [MODIFY] `scripts/kairos_daemon.py`
Add new task cycles:
- **Vault ingest sweep** (every 5 min) — process files in `vault/ingest/`
- **Gitleaks scan** (nightly) — already referenced but not fully wired
- **Obsidian sync** (hourly) — sync KI atoms to vault format
- Remove `.env` health check (already deprecated)

---

## Wave 5: Doctrine Locks

### [MODIFY] [GEMINI.md](file:///Users/pikeymickey/.gemini/antigravity/Monorepo-Uphillsnowball/GEMINI.md)
Add `<obsidian_vault_doctrine>` section referencing the vault directory structure and the operating skill.

### [MODIFY] [AGENTS.md](file:///Users/pikeymickey/.gemini/antigravity/Monorepo-Uphillsnowball/AGENTS.md)
Add vault directory to repo truth section.

---

## Open Questions

> [!IMPORTANT]
> **Pathway vs. Polling**: The `pathway_ingest.py` script references the [Pathway](https://github.com/pathwaycom/pathway) streaming framework. Do you want me to install it as a dependency, or should I use a simple `watchdog`/polling fallback? Pathway is heavy but powerful; watchdog is lightweight.

> [!IMPORTANT]
> **Aspirational docs**: I'll capture all the VS Code patches, Tauri Rust, Go ingress, C++ hot path, and Terraform FedRAMP blocks verbatim in `docs/TRUE_OBSIDIAN_ARCHITECTURE.md`. This preserves the vision without generating dead code. Acceptable?

---

## Verification Plan

### Automated Tests
- `python scripts/vault/zero_trust_pipeline.py --dry-run` — validates pipeline logic without writing
- `python scripts/kairos_daemon.py --once` — single cycle with new vault tasks
- `ruff check scripts/vault/` — lint new Python files
- `vulture scripts/vault/ --min-confidence 90` — dead code check

### Manual Verification
- Verify `vault/` directory structure exists with correct gitkeeps
- Verify updated skills have YAML frontmatter and proper descriptions
- Verify COR.KAIROS heartbeat JSON includes new vault task status

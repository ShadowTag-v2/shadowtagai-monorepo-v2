# TASK.md - Porting Claude Source Code to Antigravity Skills (STATE B)

## Scope
1. Run AST linters (biome) on `/Users/pikeymickey/Downloads/Claude_Source_Code`.
2. Extract concepts from `utils/`, `services/`, `components/`, `hooks/`, and AI modes (`buddy/`, `coordinator/`, `assistant/`, `voice/`).
3. Create SKILL.md files for the extracted modules under `.agents/skills/`.
4. Verify tools pass `mcp-fleet-vanguard`.

## State B - Clutch Mode Checkpoints
- [ ] CP-1: Run AST linter and log results.
- [ ] CP-2: Create initial SKILL.md for `ai-modes`.
- [ ] CP-3: Create SKILL.md for `services`.
- [ ] CP-4: Create SKILL.md for `hooks`.
- [ ] CP-5: Verify via `mcp-fleet-vanguard`.

---
description: Executes the secure Omega Sync protocol to push all local changes to the canonical ShadowTagAI monorepo using an ephemeral GitHub App JWT.
---

Deploy the physical python matrix script bypassing standard keychains.

// turbo-all
1. Run the Python execution script directly.
`python3 scripts/omega_sync.py`

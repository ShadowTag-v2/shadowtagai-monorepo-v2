{
  "name": "aiyou-router",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Multi-model router for Grok, Cheetah, Claude, GPT-5",
  "scripts": {
    "dev": "node --env-file=.env --loader tsx src/openai-proxy.ts",
    "start": "node --env-file=.env dist/openai-proxy.js",
    "build": "tsc -p tsconfig.json",
    "test": "echo \"No tests yet\" && exit 0"
  },
  "dependencies": {
    "express": "^4.19.2",
    "node-fetch": "^3.3.2",
    "tsx": "^4.19.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.10.0",
    "typescript": "^5.6.2"
  }
}

# tool_contracts/firebase_deploy.yaml
tool_id: "firebase_deploy"
description: "Governs the firebase_deploy operations."
risk_level: "low"
preconditions: []
evidence_requirements:
  - type: "log"
    path: ".beads/evidence/firebase_deploy.jsonl"
reuse_queries: []

# UPHILL_SCIENTIA_BLUEPRINT.md
> **AUTHORITY: The Master Unified Plan (UPHILL_SCIENTIA)**
> **STATUS:** LMTD (Locked Moat Technology Doctrine)
> **DERIVATION:** Omni-Brain (94 compiled Antigravity fragment nodes)

This document is the absolute, uncompromising engineering control-plane truth for the UphillSnowball commercial platform. It strictly enforces the Founder Wealth Matrix and the Sovereign OS integration logic over all prior backend variants.

## 1. Commercial Wedge & UX
**If the feature does not multiply revenue without multiplying effort, it is functionally dropped.**
*   **The Facade**: Hyper-premium, dark modernism ("Barney Style"). The UI completely obscures the God-Mode infrastructure (ANE, LanceDB Vectors, Orchestration DAG).
*   **Zero Ambiguity**: 1 input playground => 1 deterministic output.
*   **Monetization Engine**: Enforce the Stripe Gateway Entitlement Pattern exactly.
    - Auth (Supabase/Firebase) -> Payment (Stripe Checkout API) -> Webhook DB Entitlement (`checkout.session.completed`) -> SaaS Consumption.

## 2. Headless Ingestion Moat
Data ingestion velocity is the core product moat, executed entirely behind the scenes.
*   **Google Workspace Harvester**: A daemonized pipeline (`gemini-3.1-flash-lite-preview`) tracking live corporate drive deltas and ingesting strict SOP-A summaries directly into the local `110GB Beads` vector DB.
*   **Isolated OSINT**: `mcp-puppeteer` executing infinite-scroll logic via proxy rotations to automatically extract compliance/commercial data, completely detached from the main execution API.

## 3. The Orchestration Matrix
The "Flying Monkeys" logic is structurally decommissioned.
*   **The 5-Room Orchestrator**: Uses the `n-autoresearch` paradigm (Rust + Python). No single orchestrator bottleneck.
*   **Zero-Trust Identity**: `verify_zero_trust`. GCP standard JSON-to-Pydantic payloads validated instantly upon `/query` API entry.
*   **Temporal Dispatch**: Every API payload is pushed to `temporalio.client`. High-latency tasks are queued seamlessly into the background, maintaining sub-300ms FastAPI frontend responses.

## 4. Accelerated Edge Hardware (Aegaeon Protocol)
The infrastructure avoids Google Cloud hyper-scale burns unless physically required as a last resort.
1.  **Stage 1: Apple Neural Engine (ANE)**: For rapid, single-instruction inferences under `2,000` tokens, routed strictly to local metal via `zero_cpu_router.py`.
2.  **Stage 2: Aimdo/QJL TurboQuant**: For massive architectural mapping and RAG (e.g., loading `289MB fp16` contexts). It utilizes 4-bit Asymmetric KV caching to drop footprint to `58MB` holding a `0.998 Cosine Similarity` over accuracy.
3.  **Stage 3: Midas Monte Carlo**: Deep probability branching matrix multiplication is shunted asynchronously to raw compiled C++ (`midas_monte_carlo.cpp`).
4.  **Stage 4: Cloud Validation**: Vertex AI acts purely as the "God Mode Audit" or failback if local memory cascades critically unrecoverable loops.

## 5. Defense & Auto-Healing
*   **UI Integrity**: The `UI Consistency Auditor` explicitly intercepts build commands, blocking staging attempts if React components refer to deprecated FastAPI endpoints.
*   **160IQ Sentinel Protocol**: `cinematic_studio.py` tracks HTTP logic. If `3` successive `HTTP 500` faults trigger out of the `zero_cpu_router`, the logic bypasses the user and autonomously daemonizes `scripts/antigravity_auto_repair.py` in the background to rewrite the stack traces while the business sleeps.

# Canonical pnkln Code Pack v8.0

This manifest represents the **sole surviving code, configuration, and structural truth** extracted from the Phase 3 & 4 thread recovery sweeps. It explicitly supersedes, deprecates, and overwrites all previous verbose agent constitutions, alternate realities, and redundant configuration files.

## 1. The Surviving Set

The following 13 files constitute the complete, latest-only operational boundary. Do not read outside these files for architectural rules.

### Behavior & Rules

- `AGENTS.md` (Short, enforced behavior contract and startup script requirement)
- `docs/Cor.Constitution.v3.md` (Long-form doctrine doc)
- `.cursor/rules/cor-vibe-coding.mdc` (Compressed editor rules)
- `README_WORKSPACE.md` (The plain English bridge boundary explaining Product vs. Lab vs. Quarantine zones)

### Execution Truth

- `pyrightconfig.json` (Scoped, strictly partitioned environment declarations)
- `.vscode/settings.json` (Aggressively suppresses VS Code from indexing the quarantine zones)
- `.vscode/tasks.json` (Startup task automatically triggering the root guard)
- `scripts/pnkln_root_guard.sh` (The fatal barrier preventing modifications outside the canonical roots)

### Environmental Configs

- `apps/counselconduit/.env.example` (The solo Product runtime)
- `labs/uphillsnowball/.env.example` (The solo Lab runtime)

### Source Control Boundaries

- `.gitignore` (Quarantine, legacy, and massive datastores)
- `.ignore` (Mirrors the gitignore for ripgrep and MCP tools)

### Traceability

- `docs/MERGE_STATUS.md` (The explicit claim of full canonical merge logic, removing the partial `Pipeline` drift)

## 2. Deprecated / Eliminated Artifacts

The Prompt Repetition paradigm explicitly retired the following concepts:

- **Redundant MCP Configs:** Only `antigravity-mcp-config.json` survives. We have eliminated all `mcp_*.json` or other duplicate startup adapters that confused the agent.
- **Redundant `.env` Templates:** Smashed down to exactly one per active domain.
- **Verbose Agent Contexts:** Massive, sprawling `task.md` checklists that attempted to inject entire "constitutions" have been compressed into the active code perimeters (`AGENTS.md` and `README_WORKSPACE.md`).
- **File Copy Scripts:** Option A (Flat File Copy) script was an intermediate step; the canonical truth now relies solely on `monorepo_manifest.yaml` definitions.
- **Wildcard Pyright Resolution:** Unrestricted inference searching across `_PRE_OMEGA_BACKUP_*` and `archive/` is permanently disabled.

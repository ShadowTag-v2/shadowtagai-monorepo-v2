# AGENTS

## Mission
Keep the workspace structurally truthful, latest-only, and operational.

## Repo truth
- `monorepo_manifest.yaml` is canonical workspace truth.
- `workspace-mcp-config.json` is canonical MCP truth.
- historical adapter files are not sources of truth.

## Product split
- `apps/product_app` is the product path.
- `labs/research_lab` is the lab path.

# Architecture

Layers:

1. authority-current.json
2. authority snapshots + atoms
3. tasks + drift reports
4. generated memory bank docs
5. journal + summaries
6. code graph + retrieval
7. raw codebase

Rule:
If code conflicts with authority memory, create upgrade work and preserve authority memory.

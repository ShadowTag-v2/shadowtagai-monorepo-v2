rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /system_idempotency_keys/{key} {
      allow read: if false; // Zero trust
      allow write: if request.auth != null && request.time < resource.data.expiresAt;
    }
  }
}

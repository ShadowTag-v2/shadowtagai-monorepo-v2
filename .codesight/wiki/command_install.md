# Command_install

> **Navigation aid.** Route list and file locations extracted via AST. Read the source files listed below before implementing or modifying this subsystem.

The Command_install subsystem handles **5 routes**.

## Routes

- `GET` `warning: %s`
  `libs/cyberpunk_stack/git-lfs/commands/command_install.go`
- `GET` `Run `git lfs install --force` to reset Git configuration.`
  `libs/cyberpunk_stack/git-lfs/commands/command_install.go`
- `GET` `Git LFS initialized.`
  `libs/cyberpunk_stack/git-lfs/commands/command_install.go`
- `GET` `Only one of the --local, --system, --worktree, and --file options can be specified.`
  `libs/cyberpunk_stack/git-lfs/commands/command_install.go`
- `GET` `warning: current user is not root/admin, system install is likely to fail.`
  `libs/cyberpunk_stack/git-lfs/commands/command_install.go`

## Source Files

Read these before implementing or modifying this subsystem:
- `libs/cyberpunk_stack/git-lfs/commands/command_install.go`

---
_Back to [overview.md](./overview.md)_
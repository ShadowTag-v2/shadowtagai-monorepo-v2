# Command_locks

> **Navigation aid.** Route list and file locations extracted via AST. Read the source files listed below before implementing or modifying this subsystem.

The Command_locks subsystem handles **7 routes** and touches: cache.

## Routes

- `GET` `Error building filters: %v` [cache]
  `libs/cyberpunk_stack/git-lfs/commands/command_locks.go`
- `GET` `--cached option can't be combined with --limit` [cache]
  `libs/cyberpunk_stack/git-lfs/commands/command_locks.go`
- `GET` `--cached option can't be combined with filters` [cache]
  `libs/cyberpunk_stack/git-lfs/commands/command_locks.go`
- `GET` `--cached option can't be combined with --local` [cache]
  `libs/cyberpunk_stack/git-lfs/commands/command_locks.go`
- `GET` `--verify option can't be combined with filters` [cache]
  `libs/cyberpunk_stack/git-lfs/commands/command_locks.go`
- `GET` `--verify option can't be combined with --local` [cache]
  `libs/cyberpunk_stack/git-lfs/commands/command_locks.go`
- `GET` `Error while retrieving locks: %v` [cache]
  `libs/cyberpunk_stack/git-lfs/commands/command_locks.go`

## Source Files

Read these before implementing or modifying this subsystem:
- `libs/cyberpunk_stack/git-lfs/commands/command_locks.go`

---
_Back to [overview.md](./overview.md)_
# Command_prune

> **Navigation aid.** Route list and file locations extracted via AST. Read the source files listed below before implementing or modifying this subsystem.

The Command_prune subsystem handles **11 routes** and touches: queue.

## Routes

- `GET` `Cannot specify both --verify-remote and --no-verify-remote` [queue]
  `libs/cyberpunk_stack/git-lfs/commands/command_prune.go`
- `GET` `Invalid value for --when-unverified: %s` [queue]
  `libs/cyberpunk_stack/git-lfs/commands/command_prune.go`
- `GET` `These objects to be pruned are missing on remote:` [queue]
  `libs/cyberpunk_stack/git-lfs/commands/command_prune.go`
- `GET` `Prune error: %v` [queue]
  `libs/cyberpunk_stack/git-lfs/commands/command_prune.go`
- `GET` `Prune sub-tasks failed, cannot continue` [queue]
  `libs/cyberpunk_stack/git-lfs/commands/command_prune.go`
- `GET` `Deleting objects` [queue]
  `libs/cyberpunk_stack/git-lfs/commands/command_prune.go`
- `GET` `Unable to find media path for %v: %v` [queue]
  `libs/cyberpunk_stack/git-lfs/commands/command_prune.go`
- `GET` `Failed to remove file %v: %v` [queue]
  `libs/cyberpunk_stack/git-lfs/commands/command_prune.go`
- `GET` `failed to delete some files` [queue]
  `libs/cyberpunk_stack/git-lfs/commands/command_prune.go`
- `GET` `Prune failed, see errors above` [queue]
  `libs/cyberpunk_stack/git-lfs/commands/command_prune.go`
- `GET` `couldn't scan commits at %v: %v` [queue]
  `libs/cyberpunk_stack/git-lfs/commands/command_prune.go`

## Source Files

Read these before implementing or modifying this subsystem:
- `libs/cyberpunk_stack/git-lfs/commands/command_prune.go`

---
_Back to [overview.md](./overview.md)_

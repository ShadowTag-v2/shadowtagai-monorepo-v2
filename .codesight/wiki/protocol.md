# Protocol

> **Navigation aid.** Route list and file locations extracted via AST. Read the source files listed below before implementing or modifying this subsystem.

The Protocol subsystem handles **13 routes**.

## Routes

- `GET` `Unable to negotiate version with remote side (unable to read capabilities)`
  `libs/cyberpunk_stack/git-lfs/ssh/protocol.go`
- `GET` `Unable to negotiate version with remote side (missing version=1)`
  `libs/cyberpunk_stack/git-lfs/ssh/protocol.go`
- `GET` `Unable to negotiate version with remote side (unable to send version)`
  `libs/cyberpunk_stack/git-lfs/ssh/protocol.go`
- `GET` `Unable to negotiate version with remote side (unable to read status)`
  `libs/cyberpunk_stack/git-lfs/ssh/protocol.go`
- `GET` `no error provided`
  `libs/cyberpunk_stack/git-lfs/ssh/protocol.go`
- `GET` `server said: %q`
  `libs/cyberpunk_stack/git-lfs/ssh/protocol.go`
- `GET` `Unable to negotiate version with remote side (unexpected status %d; %s)`
  `libs/cyberpunk_stack/git-lfs/ssh/protocol.go`
- `GET` `error reading packet`
  `libs/cyberpunk_stack/git-lfs/ssh/protocol.go`
- `GET` `no status seen`
  `libs/cyberpunk_stack/git-lfs/ssh/protocol.go`
- `GET` `expected status line, got %q`
  `libs/cyberpunk_stack/git-lfs/ssh/protocol.go`
- `GET` `unexpected data, got %q`
  `libs/cyberpunk_stack/git-lfs/ssh/protocol.go`
- `GET` `unexpected flush packet`
  `libs/cyberpunk_stack/git-lfs/ssh/protocol.go`
- `GET` `unexpected delimiter packet`
  `libs/cyberpunk_stack/git-lfs/ssh/protocol.go`

## Source Files

Read these before implementing or modifying this subsystem:
- `libs/cyberpunk_stack/git-lfs/ssh/protocol.go`

---
_Back to [overview.md](./overview.md)_

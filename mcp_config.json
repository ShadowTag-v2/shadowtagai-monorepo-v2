{
  "note": "DEPRECATED ADAPTER. Canonical MCP truth lives in antigravity-mcp-config.json. This file exists only for backward compatibility with tools that read mcp_config.json.",
  "mcpServers": {
    "firebase": {
      "command": "npx",
      "args": ["-y", "firebase-tools@latest", "mcp"]
    },
    "gcloud": {
      "command": "gcloud-mcp",
      "args": []
    },
    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"]
    }
  }
}
